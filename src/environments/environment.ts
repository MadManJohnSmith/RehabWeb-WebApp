/** Desarrollo local — alineado con RehabWeb-Api (README / CORS). */
export const environment = {
  production: false,
  /** Sin barra final; rutas relativas p. ej. `/health/`, `/auth/login/`. */
  apiBaseUrl: 'http://127.0.0.1:8000/api/v1',
};
